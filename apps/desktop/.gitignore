# Generated by scripts/prepare-resources.mjs — not source.
resources/
# electron-builder output.
release/
# This package is normally pnpm-managed (see root pnpm-lock.yaml);
# scripts/prepare-resources.mjs deliberately reinstalls with plain npm right
# before packaging (pnpm's node_modules layout isn't electron-builder
# compatible — see that script's comments), which writes this lockfile as a
# disposable build byproduct, not tracked source.
package-lock.json
